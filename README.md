### remember to push repository
if you delete project floder in ccs. it will delele whole floder directly and you can't undo.
why the default setting of ccs is delete `Also delete contents on disk` and can't restore from recycle bin?
